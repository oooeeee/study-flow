version: "3.8"

services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    volumes:
      - db_data:/app/data
    environment:
      - APP_ENV=production
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

volumes:
  db_data:
